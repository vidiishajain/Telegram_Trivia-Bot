# Scenarios

**Stage 3 — Concrete end-to-end walkthroughs.** These are what you'll test against
later (stage 8). Write them as real, specific stories — actual example inputs and the
output you'd expect.

## Happy path

**Scenario:** _(name it)_
1. The user does ___ (use a real example input).
2. The agent ___.
3. The user ends up with ___.

**Expected result:** _(be specific enough to check)_

## Edge cases & tricky inputs

- **Scenario:** _(vague input)_ → expected: _(asks for clarification)_
- **Scenario:** _(very large / empty / weird input)_ → expected: ___
- **Scenario:** _(a service is slow or down)_ → expected: ___

## Done = all scenarios pass

When every scenario here behaves as written, the agent is working. Keep this list up to
date as you discover new cases — add them, then make them pass.
